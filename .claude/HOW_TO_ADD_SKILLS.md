# 📦 Как добавить архив со Skills

## Метод 1: Прямая загрузка файла

### Если у вас есть архив локально:

```bash
# Перейти в директорию skills
cd /home/user/test/.claude/skills/

# Скопировать ваш архив сюда
# Например, если архив называется skills.zip:
cp /path/to/skills.zip .

# Распаковать
unzip skills.zip

# Или если это tar.gz:
tar -xzf skills.tar.gz

# Или если это tar:
tar -xf skills.tar

# Удалить архив после распаковки (опционально)
rm skills.zip
```

## Метод 2: Через GitHub

Если архив в другом репозитории:

```bash
# Клонировать репозиторий со skills во временную директорию
git clone <url-репозитория-со-skills> /tmp/skills-repo

# Скопировать skills в проект
cp -r /tmp/skills-repo/.claude/skills/* /home/user/test/.claude/skills/

# Удалить временную директорию
rm -rf /tmp/skills-repo
```

## Метод 3: Ручное создание

Если архив небольшой, можно создать файлы вручную:

```bash
# Создать новый skill
nano /home/user/test/.claude/skills/my-skill.md
```

Формат файла:
```markdown
---
name: my-skill
description: Описание skill
tags: elixir, phoenix
---

Инструкции для Claude Code...
```

## После распаковки

### 1. Проверить структуру:

```bash
ls -la /home/user/test/.claude/skills/
tree /home/user/test/.claude/skills/  # Если установлен tree
```

### 2. Проверить формат skills:

```bash
# Каждый skill должен быть .md файлом с frontmatter
head -20 /home/user/test/.claude/skills/*.md
```

### 3. Закоммитить в git:

```bash
cd /home/user/test
git add .claude/
git commit -m "Добавлены custom skills для разработки"
git push
```

## Использование Skills

### В Claude Code:

```bash
# Просмотр доступных skills
/skills list

# Использование skill
/skill skill-name

# Например
/skill elixir-genserver
/skill phoenix-liveview
```

## Структура архива (ожидаемая)

Ваш архив должен содержать что-то вроде:

```
skills.zip
├── elixir-genserver.md
├── phoenix-liveview.md
├── ecto-schema.md
├── migration-generator.md
└── ...
```

Или с подпапками:

```
skills.zip
├── elixir/
│   ├── genserver.md
│   └── supervisor.md
├── phoenix/
│   ├── liveview.md
│   └── channel.md
└── database/
    ├── migration.md
    └── schema.md
```

## Troubleshooting

### Skills не видны в Claude Code

1. Проверьте, что файлы имеют расширение `.md`
2. Убедитесь, что есть frontmatter с `name:` и `description:`
3. Перезапустите Claude Code session

### Ошибки при распаковке

```bash
# Проверить тип архива
file your-archive.zip

# Попробовать разные команды распаковки
unzip -l your-archive.zip        # Просмотр содержимого
tar -tzf your-archive.tar.gz     # Просмотр tar.gz
```

## Нужна помощь?

1. Покажите структуру вашего архива: `unzip -l your-archive.zip`
2. Покажите ошибки при распаковке
3. Проверьте формат файлов: `head -10 /home/user/test/.claude/skills/*.md`
